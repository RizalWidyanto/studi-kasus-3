using namespace std;

class proses {
  public :
    void cetak(){
      cout<< "Masukkan jumlah pesanan Shared Hosting :";
      cin>>shrd_hstng;
      cout<<" Masukkan jumlah pesanan VPS Server :";
      cin>>vps_srvr;
      cout<< " Masukkan jumlah pesanan Cloud Server :";
      cin>>cld_srvr;
      cout<<" Masukkan jumlah pesanan Domain name :";
      cin>>dmn_nm;

    total1 = 300000 * Shared Hosting;
    total2 = 930000 * VPS Server;
    total3 = 500000 * Cloud Server;
    total4 = 280000 * Domain name;
    diskon = (total * 10 / 100);
    totals  = total - diskon; 
      }
  else if (total >= 10000000){
  diskon = (total * 10 / 100);
  totals = total - diskon;
  }
private:
//ofstream tulis_data;

int 
Shared Hosting,VPS Server,Cloud Server,Domain name,total1,total2,total3,total4,jarak,diskon,total,bayar,bayar,kembali;
}; 
